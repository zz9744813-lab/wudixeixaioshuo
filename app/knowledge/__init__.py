"""Knowledge layer (EPIC-F, spec §26, §51, §63).

The promotion ladder is a HARD gate: a rule moves up only when the concrete
requirements of §63/§51 are met; otherwise it stays at its tier with an explicit
reason. No silent promotion, no self-declared success (P-08).
"""
